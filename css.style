body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: linear-gradient(to right, pink, lightblue);
    height: 100vh; /* Assure que le gradient couvre toute la hauteur de la page */
    display: flex;
    flex-direction: column;
}